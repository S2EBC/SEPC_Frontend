import React from 'react';
import ReactDOM from 'react-dom';
import './styles/scanf.css'; // Importing the CSS file here to ensure styles are applied globally
import App from './App';

ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById('root')
);
